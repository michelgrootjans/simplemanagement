<%= show_for @transaction do |f| %>
    <%= f.attribute :date %>
    <%= f.attribute :amount %>
    <%= f.attribute :summary %>
<% end %>

<% if @transaction.bill.nil? %>
    <%= form_for(Bill.new) do |f| %>
        <%= hidden_field_tag("transaction_id", @transaction.id) %>
        <%= submit_tag "Create Bill" %>
    <% end %>
<% else %>
    <%= link_to "Bill...", bill_path(@transaction.bill) %>
<% end %>

<%= debug @transaction %>
